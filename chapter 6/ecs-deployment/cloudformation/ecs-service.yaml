AWSTemplateFormatVersion: '2010-09-09'
Description: >-
  Hospital scheduling agent on ECS Fargate behind an internet-facing ALB —
  IAM roles/cluster/networking/task/service, deployed after ecr-repository.yaml
  and an image push (task def would fail to stabilize on a missing image).

Metadata:
  AWSToolsMetrics:
    AWSAgentToolkit: aws-cloudformation@2
  com.aws.cloudformation.Context:
    arch: >-
      Browser -> ALB:80 -> Fargate task (FastAPI + Strands agent) -> Bedrock
      (Claude). Single AZ pair, HTTP only (lab/demo scope, not production).
    must:
      - ECS tasks security group only accepts inbound from the ALB security group
      - both public subnets must route 0.0.0.0/0 through an Internet Gateway (ALB requirement)

Parameters:
  AppName:
    Type: String
    Default: hospital-scheduling-agent
    Description: Prefix for resource names in this stack. Must match the AppName used for ecr-repository.yaml.
    AllowedPattern: '[a-zA-Z0-9-]*'
    MinLength: 2
    MaxLength: 60

  EcrRepositoryUri:
    Type: String
    Description: >-
      Full ECR repository URI (from ecr-repository.yaml's RepositoryUri output),
      e.g. 123456789012.dkr.ecr.us-east-1.amazonaws.com/hospital-scheduling-agent

  ImageTag:
    Type: String
    Default: latest
    Description: Tag of the image already pushed to EcrRepositoryUri.

  ModelId:
    Type: String
    Default: us.anthropic.claude-sonnet-4-5-20250929-v1:0
    Description: Bedrock model/inference-profile ID the agent invokes.

  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: VPC to deploy into (must have an Internet Gateway attached).

  PublicSubnetIds:
    Type: List<AWS::EC2::Subnet::Id>
    Description: >-
      At least 2 subnets, in different AZs, whose route table sends 0.0.0.0/0
      through the VPC's Internet Gateway. Required by the internet-facing ALB.

  ContainerPort:
    Type: Number
    Default: 8080
    MinValue: 1
    MaxValue: 65535
    Description: Port the agent's container listens on.

  ContainerCpu:
    Type: Number
    Default: 1024
    AllowedValues: [256, 512, 1024, 2048, 4096]
    Description: Fargate task vCPU units (256=.25 vCPU, 1024=1 vCPU, ...).

  ContainerMemory:
    Type: Number
    Default: 2048
    Description: Fargate task memory in MiB. Must be a valid pairing for ContainerCpu (see AWS Fargate CPU/memory table).

  DesiredCount:
    Type: Number
    Default: 1
    MinValue: 1
    Description: Number of Fargate tasks to run.

Resources:
  ExecutionRole:
    Type: AWS::IAM::Role
    Metadata:
      com.aws.cloudformation.Context:
        why: ECS uses this to pull the image from ECR & write container logs; standard AWS managed policy, no custom perms needed
        mutable: change-with-constraints
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy

  TaskRole:
    Type: AWS::IAM::Role
    Metadata:
      com.aws.cloudformation.Context:
        why: assumed by the agent code inside the container; scoped to InvokeModel only, no other AWS access
        must:
          - keep scoped to bedrock:InvokeModel*; do not widen to bedrock:* or Resource "*" beyond foundation-model/inference-profile
        mutable: review-required
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: BedrockAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - bedrock:InvokeModel
                  - bedrock:InvokeModelWithResponseStream
                Resource:
                  - arn:aws:bedrock:*::foundation-model/*
                  - !Sub arn:aws:bedrock:*:${AWS::AccountId}:inference-profile/*

  Cluster:
    Type: AWS::ECS::Cluster
    Properties:
      ClusterName: !Sub ${AppName}-cluster

  LogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub /ecs/${AppName}
      RetentionInDays: 30

  AlbSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Metadata:
      com.aws.cloudformation.Context:
        why: fronts the internet-facing ALB; 0.0.0.0/0:80 is intentional (public HTTP entrypoint), not a finding
    Properties:
      GroupDescription: !Sub ALB security group for ${AppName}
      VpcId: !Ref VpcId
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0

  EcsSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Metadata:
      com.aws.cloudformation.Context:
        why: attached to the Fargate task ENI
        must:
          - only accept inbound from AlbSecurityGroup; never widen to 0.0.0.0/0
    Properties:
      GroupDescription: !Sub ECS tasks security group for ${AppName}
      VpcId: !Ref VpcId
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: !Ref ContainerPort
          ToPort: !Ref ContainerPort
          SourceSecurityGroupId: !Ref AlbSecurityGroup

  LoadBalancer:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Name: !Sub ${AppName}-alb
      Type: application
      Scheme: internet-facing
      Subnets: !Ref PublicSubnetIds
      SecurityGroups:
        - !Ref AlbSecurityGroup

  TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub ${AppName}-tg
      TargetType: ip
      Protocol: HTTP
      Port: !Ref ContainerPort
      VpcId: !Ref VpcId
      HealthCheckPath: /health
      HealthCheckIntervalSeconds: 30
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3

  Listener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref LoadBalancer
      Protocol: HTTP
      Port: 80
      DefaultActions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup

  TaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Metadata:
      com.aws.cloudformation.Context:
        why: single-container task; image tag is a parameter so re-running `deploy-cfn.sh` after a new push updates this via stack update
        mutable: free-to-tune
    Properties:
      Family: !Sub ${AppName}-task
      NetworkMode: awsvpc
      RequiresCompatibilities: [FARGATE]
      Cpu: !Ref ContainerCpu
      Memory: !Ref ContainerMemory
      ExecutionRoleArn: !GetAtt ExecutionRole.Arn
      TaskRoleArn: !GetAtt TaskRole.Arn
      ContainerDefinitions:
        - Name: !Ref AppName
          Image: !Sub ${EcrRepositoryUri}:${ImageTag}
          Essential: true
          PortMappings:
            - ContainerPort: !Ref ContainerPort
              Protocol: tcp
          Environment:
            - Name: MODEL_ID
              Value: !Ref ModelId
          HealthCheck:
            Command:
              - CMD-SHELL
              - !Sub curl -f http://localhost:${ContainerPort}/health || exit 1
            Interval: 30
            Timeout: 5
            Retries: 3
            StartPeriod: 60
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref LogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: agent

  Service:
    Type: AWS::ECS::Service
    DependsOn: Listener
    Metadata:
      com.aws.cloudformation.Context:
        why: >-
          HealthCheckGracePeriodSeconds=120 matches the container's own
          StartPeriod=60 plus margin, so the ALB doesn't kill tasks mid-boot
        must:
          - keep DependsOn Listener; the service needs the listener registered before it can attach targets
    Properties:
      ServiceName: !Sub ${AppName}-service
      Cluster: !Ref Cluster
      TaskDefinition: !Ref TaskDefinition
      DesiredCount: !Ref DesiredCount
      LaunchType: FARGATE
      HealthCheckGracePeriodSeconds: 120
      NetworkConfiguration:
        AwsvpcConfiguration:
          Subnets: !Ref PublicSubnetIds
          SecurityGroups:
            - !Ref EcsSecurityGroup
          AssignPublicIp: ENABLED
      LoadBalancers:
        - ContainerName: !Ref AppName
          ContainerPort: !Ref ContainerPort
          TargetGroupArn: !Ref TargetGroup

Outputs:
  AlbDnsName:
    Description: Public ALB DNS name — open http://<this> in a browser
    Value: !GetAtt LoadBalancer.DNSName
  ClusterName:
    Description: ECS cluster name
    Value: !Ref Cluster
  ServiceName:
    Description: ECS service name
    Value: !GetAtt Service.Name
  LogGroupName:
    Description: CloudWatch log group (aws logs tail <this> --follow)
    Value: !Ref LogGroup
