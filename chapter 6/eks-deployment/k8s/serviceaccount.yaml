# Used only by deploy-cfn.sh. deploy.sh (the eksctl path) creates this same
# object itself via `eksctl create iamserviceaccount`.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${SA_NAME}
  namespace: ${NAMESPACE}
  annotations:
    eks.amazonaws.com/role-arn: ${SA_ROLE_ARN}
